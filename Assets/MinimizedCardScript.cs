using UnityEngine;

public class CardSummaryScript : MonoBehaviour
{
    public CrewCard crewCard;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
